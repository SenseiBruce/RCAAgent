# Deploy (optional)

AWS ECS deploy helpers. They are **not** required to build, test, or run RCA Agent.

- `terraform/` — optional Fargate stack
- `deploy.sh` — build/push image and roll the ECS service (needs AWS CLI)

See [terraform/README.md](terraform/README.md).
